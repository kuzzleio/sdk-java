language: java
os: linux
sudo: required
services:
  - docker
jdk:
  - oraclejdk8
  - oraclejdk9
  - openjdk8
before_install:
  - sudo sysctl -w vm.max_map_count=262144

script:
  - .ci/start_kuzzle.sh
  - docker run -ti golang "cd /var/app && make && cd features/java && gradle cucumber"